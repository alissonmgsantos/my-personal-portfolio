import React from 'react';
import { Profile } from '../Profile';

const Home = () => {
  return (
    <section id="home" className="banner-home">
      <Profile />
    </section>
  );
};
export default Home;
